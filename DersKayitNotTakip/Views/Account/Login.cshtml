@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Giriş Yap | Akademik Sistem</title>

    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Inter', sans-serif;
            height: 100vh;
            display: flex;
            background-color: #0f172a; /* Çok koyu lacivert zemin */
            color: #e2e8f0;
        }

        .login-container {
            display: flex;
            width: 100%;
            height: 100%;
            overflow: hidden;
        }

        /* --- SOL PANEL (Görsel Alan) --- */
        .left-panel {
            flex: 1.2; /* Sol taraf biraz daha geniş olsun */
            background: linear-gradient(135deg, #0f172a 0%, #1e1b4b 50%, #312e81 100%);
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 60px;
            position: relative;
            z-index: 1;
        }

            /* Arka plan dekoratif desenler */
            .left-panel::before {
                content: '';
                position: absolute;
                top: 0;
                left: 0;
                width: 100%;
                height: 100%;
                background-image: radial-gradient(#4f46e5 1px, transparent 1px);
                background-size: 40px 40px;
                opacity: 0.1;
                z-index: -1;
            }

            .left-panel::after {
                content: '';
                position: absolute;
                bottom: -100px;
                left: -100px;
                width: 500px;
                height: 500px;
                background: radial-gradient(circle, rgba(79, 70, 229, 0.2) 0%, rgba(0,0,0,0) 70%);
                border-radius: 50%;
                z-index: -1;
            }

        .left-content {
            text-align: center;
            max-width: 500px;
            animation: fadeIn 1s ease-out;
        }

            .left-content h1 {
                font-size: 3rem;
                font-weight: 800;
                margin-bottom: 20px;
                background: linear-gradient(to right, #fff, #a5b4fc);
                -webkit-background-clip: text;
                -webkit-text-fill-color: transparent;
                line-height: 1.2;
            }

            .left-content p {
                font-size: 1.2rem;
                color: #94a3b8;
                line-height: 1.6;
            }

        /* --- SAĞ PANEL (Form Alanı) --- */
        .right-panel {
            flex: 1;
            display: flex;
            justify-content: center;
            align-items: center;
            background-color: #1e293b; /* Koyu gri-mavi */
            padding: 40px;
            position: relative;
            box-shadow: -10px 0 30px rgba(0,0,0,0.3); /* Derinlik gölgesi */
        }

        .form-wrapper {
            width: 100%;
            max-width: 420px;
            padding: 40px;
            background: rgba(255, 255, 255, 0.03); /* Cam efekti */
            border: 1px solid rgba(255, 255, 255, 0.05);
            border-radius: 24px;
            backdrop-filter: blur(10px);
        }

        .form-header {
            margin-bottom: 35px;
            text-align: center;
        }

            .form-header .icon-box {
                width: 60px;
                height: 60px;
                background: rgba(59, 130, 246, 0.1);
                color: #60a5fa;
                border-radius: 50%;
                display: flex;
                align-items: center;
                justify-content: center;
                margin: 0 auto 20px;
                font-size: 24px;
            }

            .form-header h2 {
                font-size: 1.75rem;
                color: #fff;
                margin-bottom: 10px;
                font-weight: 700;
            }

            .form-header p {
                color: #94a3b8;
                font-size: 0.95rem;
            }

        /* Input Alanları */
        .input-group {
            margin-bottom: 20px;
            position: relative;
        }

            .input-group label {
                display: block;
                margin-bottom: 8px;
                font-size: 0.9rem;
                font-weight: 500;
                color: #cbd5e1;
                margin-left: 5px;
            }

        .input-wrapper {
            position: relative;
        }

            .input-wrapper i {
                position: absolute;
                left: 15px;
                top: 50%;
                transform: translateY(-50%);
                color: #64748b;
                font-size: 1.1rem;
                transition: color 0.3s;
            }

            .input-wrapper input {
                width: 100%;
                padding: 14px 16px 14px 45px; /* İkon için soldan boşluk */
                background-color: #0f172a; /* Input içi çok koyu */
                border: 2px solid #334155;
                border-radius: 12px;
                color: white;
                font-size: 1rem;
                transition: all 0.3s ease;
                outline: none;
            }

                .input-wrapper input:focus {
                    border-color: #6366f1; /* Morumsu mavi odak rengi */
                    box-shadow: 0 0 0 4px rgba(99, 102, 241, 0.1);
                }

                    .input-wrapper input:focus + i {
                        color: #6366f1;
                    }

                .input-wrapper input::placeholder {
                    color: #475569;
                }

        /* Button */
        .btn-submit {
            width: 100%;
            padding: 16px;
            background: linear-gradient(135deg, #4f46e5 0%, #3b82f6 100%);
            color: white;
            border: none;
            border-radius: 12px;
            font-size: 1.05rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            margin-top: 10px;
            box-shadow: 0 4px 15px rgba(79, 70, 229, 0.3);
        }

            .btn-submit:hover {
                transform: translateY(-2px);
                box-shadow: 0 6px 20px rgba(79, 70, 229, 0.4);
                background: linear-gradient(135deg, #4338ca 0%, #2563eb 100%);
            }

        .footer {
            margin-top: 30px;
            text-align: center;
            font-size: 0.8rem;
            color: #64748b;
            border-top: 1px solid rgba(255,255,255,0.05);
            padding-top: 20px;
        }

        /* Animasyon */
        @@keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Responsive */
        @@media (max-width: 900px) {
            .login-container {
                flex-direction: column;
            }

            .left-panel {
                flex: 0 0 250px;
                padding: 30px;
            }

            .left-content h1 {
                font-size: 2rem;
            }

            .right-panel {
                padding: 20px;
            }

            .form-wrapper {
                padding: 30px 20px;
                box-shadow: none;
                background: transparent;
                border: none;
            }
        }
    </style>
</head>
<body>

    <div class="login-container">

        <div class="left-panel">
            <div class="left-content">
                <div style="font-size: 4rem; margin-bottom: 20px; color: #818cf8;">
                    <i class="fa-solid fa-atom"></i>
                </div>
                <h1>Ders Kayıt ve<br>Not Takip Sistemi</h1>
                <p>Geleceğinizi planlayın. Akademik süreçlerinizi modern ve güvenli bir arayüz ile yönetin.</p>
            </div>
        </div>

        <div class="right-panel">
            <div class="form-wrapper">

                <div class="form-header">
                    <div class="icon-box">
                        <i class="fa-solid fa-user-lock"></i>
                    </div>
                    <h2>Hoşgeldiniz</h2>
                    <p>Lütfen kurumsal bilgilerinizle giriş yapın.</p>
                </div>

                <form method="post">

                    <div class="input-group">
                        <label>Kullanıcı Adı</label>
                        <div class="input-wrapper">
                            <input type="text" name="KullaniciAdi" placeholder="" required autocomplete="off" />
                            <i class="fa-solid fa-user"></i>
                        </div>
                    </div>

                    <div class="input-group">
                        <label>Şifre</label>
                        <div class="input-wrapper">
                            <input type="password" name="Sifre" placeholder="" required />
                            <i class="fa-solid fa-lock"></i>
                        </div>
                    </div>

                    <button type="submit" class="btn-submit">
                        <i class="fa-solid fa-right-to-bracket me-2"></i> Güvenli Giriş
                    </button>

                </form>

                <div class="footer">
                    &copy; 2025 Geliştiriciler<br>
                    <span style="color: #94a3b8; font-weight: 500;">Arda Çetin • Ayşenur Çakmak • Fatıma Beyza Özbay</span>
                </div>
            </div>
        </div>

    </div>

</body>
</html>